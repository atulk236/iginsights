<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>

    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="prox_bold" parent="android:TextAppearance">
        <item name="android:fontFamily">@font/prox_bold</item>
    </style>
    <style name="prox_regualr" parent="android:TextAppearance">
        <item name="android:fontFamily">@font/prox_regular</item>
    </style>
    <style name="roboto_regular" parent="android:TextAppearance">
        <item name="android:fontFamily">@font/roboto_regular</item>
    </style>
    <style name="roboto_medium" parent="android:TextAppearance">
        <item name="android:fontFamily">@font/roboto_medium</item>
    </style>

</resources>
